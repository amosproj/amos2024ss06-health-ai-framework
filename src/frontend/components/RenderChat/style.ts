import { StyleSheet } from 'react-native';

export const Style = StyleSheet.create({
  bubble: {
    maxWidth: '80%',
    borderRadius: 4,
    padding: 8,
    display: 'flex',
    flexDirection: 'column'
  }
});
