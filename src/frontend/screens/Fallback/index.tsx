import React from 'react';
import { Text, View } from 'react-native';

/**
 * Fallback screen to display until the app is ready
 */

export function Fallback() {
  return (
    <View>
      <Text>Fallback</Text>
    </View>
  );
}
